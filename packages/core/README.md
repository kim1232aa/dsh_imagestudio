# @dsh-imagestudio/dsh-image-core

契约 + 事件声明 + provider 注册表。零上游 I/O。

Requires: —

Events: `image/plan` (waterfall), `image/guard` (bail), `image/before-request` (waterfall), `image/after-result` (parallel), `image/score` (serial), `image/progress` (emit).
