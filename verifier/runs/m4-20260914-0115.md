# M4 浏览器 client bundle — 验证记录

- 日期：2026-09-14
- 分支/worktree：feat/client-bundle（work-feat-client-bundle）
- Node：v22.19.0（/tmp/node-v22.19.0-linux-x64，本机自带 v20 不满足 --experimental-strip-types，已补装）

## V1 npm test

```
# tests 158
# pass 158
# fail 0
```

✅ 全绿。基线（动手前）153/153；新增 tests/client-bundle.test.ts 5 条：

1. entry.js 互斥检测存在（开头退出 + ensureButton 内自我拆除）。
2. client 源码含全部 slot 注册点（sidebar.panellist / main key=imagestudio / 三个 toolview key / PLAN_REJECTED / open-canvas 事件）。
3. 根 package.json 的 dsh.client 声明满足宿主 parseDshClient 校验形态（platform:string、inject/external 字符串数组、immediately:boolean）。
4. 产物存在性 + closure-factory 形态静态检查。
5. 【行为冒烟】Node 内 stub window.__ModuleLoader__ + mock ctx（slots/effect）真实执行构建产物 apply()：断言 id='dsh-imagestudio'、inject=['slots','locale','connection']、main/tool.call.toolview 两个 inject 作用域、sidebar.panellist id/order/label、main key='imagestudio'、toolview 三个 key 全部注册。产物缺失时 4/5 降级为 warn-skip（lib/ 在 .gitignore 内，属构建产物）。

## V2 npm run build:client

✅ 成功。产物 `packages/client/lib/client.js`（16.26 kB，gzip 5.13 kB）+ sourcemap。
形态核对：文件头 `window.__ModuleLoader__.load({ id: "dsh-imagestudio", factory: (require) => {`，尾 `return module.exports; } });`；`react/jsx-runtime` 经注入 require 解析（external），其余全部内联；导出 `apply` / `inject` / `name`（named exports）。

环境坑（已解决，供复现）：本环境 /mnt 文件系统上 npm 解包随机丢文件（cordis/lib/index.js、typescript/lib/tsc.js 等），且不支持 bin symlink。解法：/tmp 本地目录 `npm install --no-bin-links` 后 cp 回 worktree 并 diff 校验补齐；build:client 脚本用 `node node_modules/tsdown/dist/run.mjs` 直调，绕开 .bin。

## V3 真实壳内验证（dsh 0.1.5-rc.1 web）

⏳ 待人工/Stage 4。本机未安装 @deepseek-ai/dsh CLI，slot 渲染（sidebar.panellist 投影、main keyed 面板、tool.call.toolview 接管）未在真实壳内跑过。slot 契约依据 DeepSeek-Harness 仓库源码（ui-sidebar/ui-tool/ui-layout 的 client/contract/slots.ts 与 ui-conversation apply.ts 的注册模式），非猜测 API。
降级路径已备好：bundle 不可用或挂载失败时 entry.js 兜底照旧；localStorage `istudio:client-disabled=1` 可强制兜底。

## 变更清单

- 新增 packages/client/：package.json、tsconfig.json、tsdown.config.ts、src/{index.ts, slots-face.ts, locales.ts, sidebar.tsx, panel.tsx, toolview.tsx}
- packages/ui/src/entry.js：开头互斥退出 + ensureButton 内自我拆除（仅此一处动 packages/ui）
- 根 package.json：exports["./client"] → packages/client/lib/client.js；dsh.client 补全；scripts.build:client；devDeps +tsdown/typescript/react/@types/react/@types/node
- 根 tsconfig.json：exclude packages/client（自带 tsconfig，root typecheck 基线本身已有 TS5097 等存量错误，未引入新错误）
- tests/client-bundle.test.ts 新增
- README.md 新增「浏览器 client bundle（M4）」一节
- 未触碰 packages/core、packages/skills、packages/tools、studio-page.ts

