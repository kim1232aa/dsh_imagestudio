# M1 验收运行记录 — feat/foundation（coder-foundation）

日期：2026-09-14 · 分支：feat/foundation · Node：v22.19.0（/tmp/node-v22.19.0-linux-x64/bin）

## 环境

- worktree：`$HOME/work-feat-foundation`（主仓 `/mnt/agents/output/work/dsh_imagestudio` 所在挂载点
  不支持 symlink，`npm install` 在 /mnt 下报 ENOTSUP，故 worktree 落在 $HOME）
- `npm install`：exit 0（package-lock.json 补齐 jpeg-js 条目，resolved 已指回 registry.npmjs.org）

## 基线（改动前）

```
$ npm test        # exit 0 — # tests 153 / # pass 153 / # fail 0
```

## 改动后验证

```
$ npm test        # exit 0 — # tests 164 / # suites 34 / # pass 164 / # fail 0
$ npm run accept  # exit 0 — 15/15 PASS（含新增 AC-UI-08-gate: status=422 code=PLAN_REJECTED；
                  #          AC-UI-08-gen 改为 force:true still generates）
```

## PLAN_REJECTED 实际报错样例（MemoryWebServer + MiniTools）

工具层（istudio_generate，plan veto、无 force）：

```
code=PLAN_REJECTED
message=PLAN_REJECTED: score 0/82; failures: veto:明显 CG / 游戏宣传图; veto: 明显 CG / 游戏宣传图
```

HTTP 层（POST /imagestudio/api/generate，无 force）：

```
HTTP 422 {"jobId":"…","error":{"code":"PLAN_REJECTED","score":0,"threshold":82,
  "failures":["veto:明显 CG / 游戏宣传图"],"veto":"明显 CG / 游戏宣传图"}}
```

## 已知不通过项（均非本次引入）

- `npm run typecheck`：HEAD 起即失败——tsconfig `types:["node"]` 但未装 @types/node（TS2688）；
  试装 @types/node 后暴露出大量既存错误（全部测试文件 TS5097 `.ts` 扩展名导入、
  ui-routes 等 TS2352 类型断言），与 M1 改动无关，未纳入本模块修复范围。
- AC-UI-01（官方壳侧栏按钮人工点击）沿用旧结论：未签字。
