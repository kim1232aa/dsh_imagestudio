# M2 验证记录 —— skill 聊天自动匹配（coder-skills）

- 时间：2026-09-14 01:00 (UTC)
- 分支：feat/skills-chat（worktree: /mnt/agents/output/work-feat-skills-chat）
- Node：v22.19.0（/tmp/node-v22.19.0-linux-x64/bin）

## 变更范围

1. `packages/skills/src/schema.ts`：`SkillPreset` 新增 `triggers: string[]`（必填，5-12 个非空中文字符串）与 `antiTriggers?: string[]`（可选，存在则逐条校验非空字符串）；`validatePreset` 同步校验，known 字段集合同步；错误信息含目录名便于定位。
2. `skills/*/preset.yaml` ×5：补 6-10 条 triggers + 2-4 条 antiTriggers（取自 research/skills-analysis.md §4）。
3. `packages/skills/src/bundled.ts`：5 个内嵌兜底 preset 同步补 triggers/antiTriggers。
4. `skills/*/chat.md` ×5（3.0-3.6KB，≤6KB）：何时使用（含兄弟 skill 边界）→ 创作工作流（istudio_skill_plan → 检查 selfCheck → istudio_generate(planId)，明示「不要自己编造工具」）→ 硬约束摘要（21:9 / 3:4 / 9:16 / NO WAX SKIN / 82 分 / 80 分 / 梵想美学等）→ PLAN_REJECTED 应对话术。
5. `packages/skills/src/index.ts`：apply 时经 `ctx.get('skills', false)` 探测可选 SkillRegistry（不进 inject）；存在则对每个启用 skill 读 `<dir>/chat.md` 并 `register({name, description: "${title}——触发：前3个trigger…等", content, invocation: {modelInvocable: true, userInvocable: true}})`；注册包进 `ctx.effect`（register 返回的 disposer 作为清理）；chat.md 缺失时 console.warn 并跳过该 skill。
6. `packages/skills/src/load.ts`：修复 `enabled: []`（cordis Config 对缺省数组的归一化结果）把全部 skill 过滤掉的问题——空白名单视为不过滤。
7. `plugin.ts`：仅替换 systemPrompt 文字段——5 个 skill 的聊天触发场景、plan→检查→generate 标准链路、PLAN_REJECTED 红线（解释原因并询问改 brief 还是 force:true）、不重复注册说明；保留 generate_image / dsh-imagegen / 技能台 字样（coexist 测试依赖），组合器结构未动。
8. `tests/skill-chat-register.test.ts`（新增，9 个用例）：mock SkillRegistry 验证 5 skill 注册、invocation 双开、description 含触发摘要、content 含关键约束词（21:9 / 3:4 / 哈苏·徕卡·理光 / 9:16·梵想美学·80 / NO WAX SKIN / PLAN_REJECTED）；effect 清理计数；无 SkillRegistry 时插件仍可用；chat.md 缺失跳过不崩溃；triggers 校验（缺失/数量越界/非字符串/antiTriggers 可选/磁盘与 bundled 全量 5-12 条/封闭 schema）。
9. `tests/alignment.test.ts`：两个 validatePreset 夹具补 triggers 字段（AC-SK-02、AC-CP-07），断言目标不变。

## 结果

- `npm test`（node --test --experimental-strip-types tests/*.test.ts）：**162 tests / 35 suites / 162 pass / 0 fail**。
- 基线（变更前）：153 pass / 0 fail；新增 9 用例全绿。

## 偏差说明

- 环境路径：实际仓库位于 /mnt/agents/output/work/dsh_imagestudio（非 $HOME/dsh_imagestudio）；npm install 需 `--no-bin-links`（该文件系统不支持 symlink）。
- SPEC §2 M2 原文称 triggers「可选」，但 SPEC §3 契约与任务卡要求必填 5-12 个——按必填实现。
- chat.md 按上游 skill 原文写 movie-poster 画幅 9:16、阈值 80；现有 preset.yaml 的 poster mode 仍为 3:4、阈值 82（属 M1/预设演进范畴，本次未改动画幅锁定逻辑，已在 chat.md 中按创作规范描述）。
