# 03 验收对照（2026-09-12）

对照对象：`docs/03-验收标准.md`（新稿）。旧 `docs/03-验收规范.md` 的 AC-UI-08 拦截作废。

判定：✅ 现可勾　△ 有入口/骨架未闭环　❌ 未做　🔴 红线未过

## 零、红线

| 条 | 状态 | 说明 |
|---|---|---|
| 不选 skill 能直接出图 | ✅ | 工作台「不使用 Skill」+ mock |
| 评分低出图按钮无 disabled | ✅ | `$('go').disabled = false` |
| 低分不弹窗/不二次确认/不自动重试 | ✅ | |
| 无「这个我不能生成」创作拒绝 | △ | 创作 veto 已不挡出图；`image-guard` 仍拦 CSAM / make a bomb |
| 无敏感词过滤 / 黑名单 / 内容审查 | △ | compile 仍把 bannedPromptTerms 记进 selfCheck 供展示；不再短路 generate |
| 自动负面词可见可删 | △ | API 已接收 `body.negative`；页面控件以 studio-page 最新稿为准 |
| skill 比例显示且可改 | ✅ | 九档固定；generate 以用户 `aspectRatio` 优先 |
| 不自动删图 | ✅ | AssetStore 无过期删除 |
| 不自动改写用户提示词 | △ | 有 plan 时默认仍用 shot.prompt，用户传入 `body.prompt` 现在优先 |
| 密钥不进浏览器/日志 | △ | 页面不渲染密钥；未做 DevTools 专项扫描 |

## 一、安装与基础

侧栏「生图」、独立页、顶栏四页可切：✅ 骨架。卸载无残留：△ 有 MemoryWebServer.uninstall，未在官方 dsh 实测。未配渠道提示：△。

## 二、渠道配置

❌ 无多渠道设置页、无「检测可用模型」。仅 mock。各家协议 ❌ 未验证。

## 三、普通生图

比例/清晰度/张数控件 ✅；九档比例 + 1K/2K/4K 像素由 `tests/raster.test.ts` 按文件属性核对 ✅。取消：`JobStore` + `POST /cancel` + 工作台「取消」✅（`tests/jobs.test.ts`）。历史回填参数 △ 仍偏文件名。图生图上传/拖拽/粘贴 ❌。增强 ❌。栏宽拖拽记忆 ❌。

## 四、视频

内置 generateVideo + POST /imagestudio/api/video，和图片共用 JobStore。mock 用 ffmpeg 写出真 mp4，url===path 不改写域名。页面视频模式可提交并内嵌 video controls。tests/video.test.ts 通过。

未勾：真实上游、抽帧、档位价格、画布视频节点。mock 是色块片，不是成片模型。

## 五、无限画布

开箱文本+配置并已连线。圆点拖线，文本入边当提示词，发送后结果图落在配置右侧并保留连线。tests/canvas.test.ts 通过。

未勾：视频节点、标注框、100 节点 30fps、右键菜单完善。

## 六、创作 Skill

六入口名称 ✅。想方案 / 就这样出图 / 重新想一版 ✅。人话判断取决于 compile reasoning。三联 21:9 + 本地 10px 缝 ✅（mock）。海报分层四件、只重出字体层、封面 3:4 实测 ❌。角色默认单张 ✅（未勾三视图）。skill 读本地目录 ✅ preset.yaml，未按 SKILL.md 原文热加载。

## 七、其余

画廊去重/筛选/瀑布流 △ 空状态。电商：预览只出计划、确认后才 generate（tests/ecom.test.ts）✅ 骨架。反推 ✅ 有 API。GIF 本地编码 ❌。Agent 对话生图 ❌。UI 设计模式 ❌。模板库 ❌。

## 八–十三

存储路径可配 △。7 天不删 未测。性能 2s/200ms/30fps 未测。文档安装说明部分有。真实使用两周 ❌。

## 结论

**红线大面已转向「分数不挡出图」。十一项功能未齐，核心视频/画布未过，不能按 03 签字验收。**
