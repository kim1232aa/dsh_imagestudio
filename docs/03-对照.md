# 03 验收对照（2026-09-12，对新稿诚实勾选）

依据 `docs/03-验收标准.md`。未勾 = 未达到。不许把骨架写成通过。

## 零、红线

- [x] 不选 skill 也能直接出图（默认 skillId 空，走 prompt）
- [x] 评分低时「就这样出图」没有 disabled
- [x] 评分低不弹窗、不二次确认、不自动重试
- [x] 工作台没有「这个我不能生成」文案
- [x] 没有创作向敏感词/黑名单拦出图；compile 仍会给 veto **打分**（展示用）。`image-guard` 已改为直通扩展点，不内置任何拦截规则（含此前仅有的 CSAM / make a bomb 两条正则，应红线要求已全部移除）
- [x] 负面词输入框可见，skill 方案带入后可删，generate 传 `negative`
- [x] skill 建议比例会写进界面，用户点别的比例能改
- [x] 无自动删图逻辑
- [x] 普通出图不改写用户 prompt；想方案才生成新提示词
- [x] 密钥走宿主，页面只有 mock/渠道状态

## 一、安装与基础

- [x] 独立页 `/imagestudio` 能打开
- [x] 顶栏四页可切
- [ ] 真 dsh web 侧栏「生图」需在用户机器装插件后验（预览环境曾通）
- [x] 未配渠道时设置页本身（工作台「设置」页可填渠道 / 检测模型；密钥只写 env 名）
- [ ] 卸载无残留：单测 AC-UI-11 路由会卸；真 dsh 未做完整卸载观察

## 二、渠道

- [x] mock 渠道可出图
- [ ] 多渠道表单 / 检测可用模型 / OpenAI / 异步轮询 / Seedream / Qwen / Gemini / Grok Imagine / GLM / MiniMax：未做或未用真密钥验

## 三、普通生图

- [x] 文生图 mock 出图
- [x] 9 档比例选项在界面（未用像素属性逐档核对）
- [x] 张数 1–4 选项在界面
- [x] 图生图拖拽/粘贴/大小上限（10MB）
- [x] 取消真 abort、幽灵任务清理（JobStore + failRunningOnBoot）
- [x] 全屏预览缩放翻页（0.5x–3x，← →）
- [x] 结果六动作齐（下载/加画廊/加对话/转参考/复制提示词/重生成）
- [x] 灵感案例点击回填
- [x] 「随机」换一条
- [x] 对话面板可展开
- [ ] 栏宽拖拽记忆、提示词增强真模型

## 四、视频 / 五、画布 / 七、其余

- [x] 视频协议内置出片、URL 原样、抽帧（mock；无 ffmpeg 时用内置 mp4 封装）
- [x] 无限画布：文本/配置/图片/视频节点、连线、缩放、localStorage 保存、结果落在右侧
- [x] 电商：先 preview 再 confirm 批量
- [x] 画廊：结果卡入库 + 按路径去重 + 从 assets 回填
- [x] GIF 本地编码（ffmpeg 优先，否则纯 JS GIF89a）
- [ ] Agent 对话生图、UI 切图、模板库：**未实现**

## 六、创作 Skill

- [x] 六个入口名分开（三联封面 ≠ 电影海报）
- [x] 「想方案」出 plan + 分数 + 人话判断（取决于 compile reasoning）
- [x] 低分仍可出图
- [ ] 三联实测 21:9 像素误差 ≤1%（mock 图未按像素比断言）
- [x] 本地拼接 10px 黑缝（compose 实现在，单测覆盖「不让模型画三格」）
- [ ] 海报四件套 + 只重出字体层 + 调原作者脚本
- [ ] 角色默认不出三视图的生成路径未单独验

## 十、性能 / 十三、两周真用

- [ ] 未做 2s/30fps/500 图/两周真实产量验收

**结论：红线 + 普通生图 mock + 视频/画布/电商/GIF 的 mock 通路已能点；真密钥多渠道与官方壳 AC-UI-11/12 仍未签字。**
