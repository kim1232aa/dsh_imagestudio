# Generated by scripts/gen-dsh-patch.mjs — regenerate after moving the repo.
# Load into official DeepSeek Harness:
#
#   npx @deepseek-ai/dsh web --patch /home/workdir/artifacts/dsh_imagestudio/examples/dsh-web.patch.yml
#
# Then open http://127.0.0.1:3080 → Settings → Plugins.
# Expected ACTIVE fibers: image-core, image-compose, image-guard, image-assets,
# image-skills, image-provider-mock, image-tools.
# Chat tools: image_skill_plan, image_generate, image_edit, image_describe,
# image_compose, image_assets.

- insert:
    - id: image-core
      name: '/home/workdir/artifacts/dsh_imagestudio/packages/core/src/index.ts'

    - id: image-compose
      name: '/home/workdir/artifacts/dsh_imagestudio/packages/compose/src/index.ts'

    - id: image-guard
      name: '/home/workdir/artifacts/dsh_imagestudio/packages/guard/src/index.ts'

    - id: image-assets
      name: '/home/workdir/artifacts/dsh_imagestudio/packages/assets/src/index.ts'
      config:
        workspaceRoot: !!js process.cwd()
        keepLastTasks: 50

    - id: image-skills
      name: '/home/workdir/artifacts/dsh_imagestudio/packages/skills/src/index.ts'
      config:
        dir: '/home/workdir/artifacts/dsh_imagestudio/skills'
        enabled:
          - cinema-dna-21x9x3
          - life-force-portrait
          - photography-simulation
          - movie-poster
          - character-casting

    - id: image-provider-mock
      name: '/home/workdir/artifacts/dsh_imagestudio/packages/provider-mock/src/index.ts'

    - id: image-tools
      name: '/home/workdir/artifacts/dsh_imagestudio/packages/tools/src/index.ts'
      config:
        limits:
          concurrency: 3
          perTaskTimeoutMs: 180000
          maxImagesPerCall: 4
