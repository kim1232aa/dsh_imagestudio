# Official bundle layer. Activated by `dsh plugin add file:<this-repo>`
# because package.json declares dsh.bundle.patch.
#
# One row is enough: index.ts apply() mounts the design-doc packages as
# child fibers. Requires the host profile to already provide `tools`
# (and `llm` / `jobs` for skills + assets). The shipped web profile does.
- insert:
    - id: image-studio
      name: dsh-imagestudio
